# 环境配置
ENV=development

# API Key认证 - 生产环境请设置强密钥
# 生成方式: python -c "import os; print(os.urandom(32).hex())"
API_KEY=your-secure-api-key-here

# 服务配置
HOST=0.0.0.0
PORT=8080

# CORS允许的来源（逗号分隔）
ALLOWED_ORIGINS=http://localhost:8080,http://127.0.0.1:8080,http://localhost:8000,http://127.0.0.1:8000
